# 2026-08-25 工作日志（rain-portfolio）

## 03:2x 用户再次要求"重做 CF 优选"
- **澄清事实**：CF 优选其实从未真正 apply 过（之前只分析方法论 + 写了 cf-optimize.py / cf_prefer_local.ps1 脚本，因中途发现站点实为 GitHub Pages 而非 CF Pages，原方案前提错，停在"写了没执行"）。用户误以为已生效。
- **沙箱网络**：到 api.cloudflare.com 仍完全不通（HTTP 000、DNS 解析失败），故无法从沙箱调 CF API。所有 CF 改动必须由用户在**本机**跑脚本（本机网络能通 CF）。
- **动作**：升级 `cf_prefer_local.ps1`——改用 `curl.exe --resolve` 测真实 HTTPS 时延（比原 TCP 连接测速准），并加了"全部不可达即判定为网络彻底封 CF、hosts 无解"的兜底提示。仍只影响本机 hosts。
- **现状根因**：ovorain.com 经 CF 边缘，节点曾从 HKG(香港)切到 NRT(日本)，国内直连不稳；用户当前靠代理才能开（ERR_TUNNEL_CONNECTION_FAILED 多为代理 tunnel 也连不上目标）。
- **未决**：R2 路线仍暂停（用户暂无法绑卡，PayPal+国内纯银联储蓄卡不可行，需 Visa/MC 双币卡或 Wise/Revolut/Depay 虚拟卡）。COS ¥3.60 CDN 流量包(100GB/12月)购买状态未最终确认（用户截图停留在购买数量=0，未付款）。
- **待办**：用户在本地管理员 PowerShell 跑 cf_prefer_local.ps1 验证本机能否不开代理直连；若全不可达则只能走代理或迁国内托管。

## 关键认知（易踩坑）
- CF 优选(host/SAAS)是免费 band-aid，不能根治国内访问；真·可靠是国内托管(需备案)。
- COS"外网下行流量包"才是挡欠费的（访客下载视频产生），"CDN 回源流量包"挡不住——用户曾两次选错。
- 沙箱 IP 被 Cloudflare 拦，凡 CF API 操作必须用户在本地执行。
