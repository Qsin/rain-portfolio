# 2026-08-24 工作日志

## 00:10 修复 www 跳转多跳（CF Single Redirect Rule）
- 用户生成带 **Rulesets:Edit** 权限的新 token（cfat_FKkC…），覆盖本地 `cf_token.txt`（已被 .gitignore 忽略，不上传）。
- 用脚本建 CF Single Redirect Rule（phase=http_request_dynamic_redirect，ruleset id `261460ef30d34ca8bf1e7ca9b5e392c5`）：
  - expression = `http.host eq "www.ovorain.com"`
  - 301 → `concat("https://ovorain.com", http.request.uri.path)`，保留 query。
- 实测验证（curl）：
  - `www.ovorain.com` → 301 `https://ovorain.com/`（一步到位，不再 http→https 多跳）
  - `www.ovorain.com/test-path` → 301 `https://ovorain.com/test-path`（动态拼接 OK）
- 临时脚本 `_cf_redirect.py` 已删除。
- 结论：ovorain.com 为主、www 兜底一步到 https，访问体验最优。新 token 权限足够，以后 CF 规则类改动均可由我直接 API 操作。

## 16:30 R2 迁移路线确认（替代 COS）
- 用户拍板"试试 R2"：COS 按量欠费停服（已欠 ¥0.15，明天 8 点停服、9/8 数据销毁）；R2 免费额度足够（10GB 存储 + 千万次/月读取 + 10GB 出口流量），个人站永久免费。
- 已澄清误解：
  - COS「公有私读写」必须保持——视频 URL 被 index.html 匿名引用，改私有会全部 403；豆包建议的"改私有"是错的。
  - 真防盗链走 CF Referer 规则（不是改桶权限）。
- 已确认 TMT（机器翻译）已关闭：截图显示「开通后付费」「TMT 服务」两个开关都灰关，且顶部提示「您已关闭后付费模式……不可用」——不会再扣 TMT 钱（TMT 是 2024 老资源，与本流程无关）。
- R2 上传环境就绪：
  - 项目根 `r2_migrate.py` 写好：本地 18 个 mp4 → R2 保持原 key → 改写 index.html 前缀 → 幂等。
  - 隔离 venv：C:\Users\Miss C\.workbuddy\binaries\python\envs\default 已装 boto3。
  - .gitignore 加 r2_secret.txt（凭据不上传）。
- 阻塞：用户需在 dash.cloudflare.com → R2 → 激活（绑卡不扣费）→ 建桶（如 ovorain-video）→ 开 Public → 创建 API 令牌（对象读写、绑指定桶）。需提供 5 项：account_id / access_key_id / secret_access_key / bucket / public_base。
- 回退窗口：建议用户腾讯云主账号充 ¥1 让 COS 续命到 R2 验收（防 R2 翻车回不去）。

## 17:00 COS 资源包选错纠正
- 用户混淆「存储容量包」与「CDN 流量包」：视频播放产生的是 CDN 流量费（按下载字节数），不是存储费（按占用容量）。80MB 视频怎么播都花不了多少存储钱，但每完整播一次就产生等量 CDN 流量费。
- 正确选择：勾「流量 → CDN 流量包 → 100GB/6月/¥20」，**不**勾「存储容量 → 标准存储容量包 → 10GB/¥0.85」。
- 坑提醒：若用户已点「立即购买」付错成 ¥0.85 存储包，腾讯云不自动退（需工单）。
- 用户最终走 R2，不需买任何 COS 包。COS 桶明天 8 点停服前要么迁移 R2 完成、要么充钱续命。
